/*
Array is a collection of same data types in one memory allocation

for example 5 numbers can be stored in one array in one memory location in sequential order
*/

#include <iostream>

int main() {
    int numbers[4] = {2, 5, 1, 6};

    std::cout << numbers[1]; // in the brackets there will be written number of element

    return 0;
}